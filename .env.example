# Worktree Manager Environment Configuration
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will not be committed

# =============================================================================
# GitHub Configuration
# =============================================================================

# GitHub personal access token (required for cloning/fetching repositories)
# Create one at: https://github.com/settings/tokens
# Required permissions: repo (for private repos), read:org (for org repos)
GITHUB_TOKEN=your_github_token_here

# GitHub organization or username (default: timcarrender04)
GITHUB_ORG=timcarrender04

# =============================================================================
# Repository Path Configuration
# =============================================================================

# Root directory where repositories are located (container path)
# Default: /repos
REPO_ROOT=/repos

# Host path for repositories (where repos are located on the host machine)
# This is used for path translation when accessing from host (e.g., VS Code)
# Default: same as REPO_ROOT
HOST_REPO_ROOT=/home/tim-175/repos

# Root directory for worktrees (where worktree directories are created)
# Default: {REPO_ROOT}/Tree
WORKTREE_ROOT=

# =============================================================================
# Repository Names (Optional - only override if using different repo names)
# =============================================================================

# Frontend repository name
FRONTEND_REPO=sideline-frontend

# Backend repository name
BACKEND_REPO=sideline-backend

# Viewer repository name
VIEWER_REPO=ohif-viewer

# =============================================================================
# Repository Keys (Optional - only override if using different keys)
# =============================================================================

# Repository keys used in the API (for backward compatibility)
FRONTEND_KEY=frontend
BACKEND_KEY=backend
VIEWER_KEY=viewer

# =============================================================================
# Supabase Configuration
# =============================================================================

# Supabase project URL
# Find it in: Supabase Dashboard → Project Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous/public key (safe to expose)
# Find it in: Supabase Dashboard → Project Settings → API → anon/public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase service role key (⚠️ Keep this secret!)
# Find it in: Supabase Dashboard → Project Settings → API → service_role key
# Used for admin operations that bypass RLS (Row Level Security)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# =============================================================================
# Docker Configuration
# =============================================================================

# Linux user ID for Docker container (should match your host user ID)
# Check your user ID with: id -u
# Default: 1020
USER_ID=1020

# Linux group ID for Docker container (should match your host group ID)
# Check your group ID with: id -g
# Default: 1020
GROUP_ID=1020

# =============================================================================
# AWS Configuration (Optional - if using AWS services)
# =============================================================================

# AWS Access Key ID
AWS_ACCESS_KEY_ID=

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=

# AWS Region (e.g., us-east-1, us-west-2)
AWS_REGION=

# =============================================================================
# Varus Hill Configuration (Optional - if using Varus Hill service)
# =============================================================================

# Varus Hill service token
VARUS_HILL_TOKEN=

# =============================================================================
# Notes
# =============================================================================
# 
# Alternative GitHub Token Locations:
# If GITHUB_TOKEN is not set, the application will look for token files in:
# - {REPO_ROOT}/../.github-token
# - {REPO_ROOT}/../token
# - {REPO_ROOT}/../GITHUB_TOKEN
# - {REPO_ROOT}/.github-token
# - {REPO_ROOT}/token
# - {REPO_ROOT}/GITHUB_TOKEN
#
# Docker Compose:
# The docker-compose.yml file automatically loads variables from .env
# Make sure to restart the container after changing .env values
#
# Next.js:
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# All other variables are server-side only
